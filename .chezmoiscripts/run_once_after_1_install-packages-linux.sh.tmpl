{{ if eq .chezmoi.os "linux" -}}
#!/bin/bash
set -euo pipefail

{{ range .packages.linux.scripts -}}
if ! {{ .test }} &>/dev/null; then
  echo "==> Installing {{ .name }}..."
  {{ .cmd }}
fi
{{ end -}}
{{ end -}}
